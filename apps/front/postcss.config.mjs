const config = {
  // Tailwind CSS v4 requires the dedicated PostCSS plugin
  // Using the object form ensures PostCSS loads the plugin correctly
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
